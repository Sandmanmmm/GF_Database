# GameForge Database Configuration
# Development Environment

# Database Connection
DATABASE_URL=postgresql://gameforge_user:securepassword@localhost:5432/gameforge_dev
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gameforge_dev
DB_USER=gameforge_user
DB_PASSWORD=securepassword

# Connection Pool Settings
DB_POOL_SIZE=20
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# SSL Settings (Development)
DB_SSL_MODE=prefer

# Migrations
MIGRATE_URL=postgresql://gameforge_user:securepassword@System.Management.Automation.Internal.Host.InternalHost:5432/gameforge_dev

# Backup Settings
BACKUP_RETENTION_DAYS=30
